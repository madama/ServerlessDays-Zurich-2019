language: minimal

install: true

before_script: pip install --user awscli

script: aws s3 sync . s3://serverless-days-zurich --exclude .git --exclude .travis.yml --exclude README.md

notifications:
  email:
    - d.madama@gmail.com
    - stefano.cicatiello@xpeppers.com